ref 与 out区别：

使用ref型参数时，传入的参数必须先被初始化。
对于out而言，必须在方法中对其完成初始化 

out适用在需要return 多个返回值的地方，
而ref则用在需要被调用的方法修改调用者的引用的时候

ref参数在传之间必先赋值，在计算机内部 是用指针来实现的

out参数在函数中必须赋值后才能返回

表达式及对象的属性不能作 ref及out参数

ref 和 out 底层类似

显著的区别就是 一个传进来 一个传出去