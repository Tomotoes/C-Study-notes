当程序启动时，默认情况下，显式创建的线程 是 前台线程。

通过手动的设置threadTwo对象的IsBackground属性为 true来创建一个后台线程。

第一个线程完成后，程序结束 并且后台线程被终止。这是前台线程 与 后台线程的主要区别

进程会等待所有的前台线程完成后在结束工作，

但是如果只剩下后台线程，则会直接结束工作

一个重要注意事项是 如果程序定义了一个不会完成的前台线程，主程序并不会正常结束。