多线程的概念：
                我们平时程序运行起来，我们就称为一个进程，
                但这一个进程里面 它要做很多事情，这些事情要同时进行，每件事情称为一个线程，
                线程实际上是要执行的任务
            ·进程Process
            ·线程Thread
                线程中的指令：一个方法(委托)
                线程中的数据：相关的对象
