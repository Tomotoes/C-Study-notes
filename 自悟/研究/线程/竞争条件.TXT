当多个线程同时访问一个 方法时，非常有可能会出错，因为线程互相 在做自己的事情。

这种情形 被称为竞争条件(race condition)。

竞争条件是多线程环境中非常常见的导致错误的原因。

为了 确保不会发生 以上情形，

必须保证当 有线程操作方法时，所有其他线程必须等待 直到 当前线程完成操作。

我们可以使用Lock关键字来实现这种行为。

如果锁定了一个对象，需要访问该对象的所有其他线程 则会处于阻塞状态，并等待 知道该对象解除锁定。这可能会导致严重的性能问题。